import React from 'react';
import { Button } from 'semantic-ui-react';
import './style.scss';

// import local

const ValidButton = () => (
  <div>
    <Button className="ValidButton mobileValidButton" positive>Validé</Button>
  </div>
);

export default ValidButton;
