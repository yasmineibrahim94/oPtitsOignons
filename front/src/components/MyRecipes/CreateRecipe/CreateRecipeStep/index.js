import React from "react";

import axiosRequest from "../axiosRecipe/axiosRequest";

let counter = 0;
const addStep = () => {
/*   console.log("addStep")
  counter += 1;
  const target = document.querySelectorAll("#ContainerTextArea");
  console.log(`target`, target)
  target.innerHTML = `
    <div id={"stepContainer "${counter}}>
      <textarea className={"step-text step " ${counter}} maxLength="550">
        étape ${counter}
      </textarea>
      <button
        class={"delButton step "${counter}}
        onClick={() => (this.parentElement.innerHTML = "")}
      >
        {" "}
        suprimer{" "}
      </button>
    </div>` */
};
/* value=${this.state.description}
        onChange=${this.onDescription}*/

export default addStep;
